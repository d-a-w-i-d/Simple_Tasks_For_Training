print "Set range of numbers between which script will print all numbers \
that are divided by 5"
user_start = int(raw_input("Set start point: "))
user_stop = int(raw_input("Set stop point: "))
for num in range(user_start, user_stop + 1):
    if num % 5 == 0:
        print num
